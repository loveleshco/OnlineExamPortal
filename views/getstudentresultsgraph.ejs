 <html>
  <head>
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript">
      google.charts.load('current', {'packages':['bar']});
      google.charts.setOnLoadCallback(drawChart);
<% var i= 0; %>
      function drawChart() {
        var data = google.visualization.arrayToDataTable([
          ['Student', 'Tnf','mcq','desc','total'],
         <% for(i=0;i<lenn;i++){ %> 
          ['<%= mcqmarks[i].studentid %>', <%= mcqmarks[i].mcqstudentmarks %>, <%= tnfmarks[i].tnfstudentmarks %>, <%= descmarks[i].marks %>,<%= mcqmarks[i].mcqstudentmarks %> + <%= tnfmarks[i].tnfstudentmarks %> + <%= descmarks[i].marks %>],
          <% } %>
        ]);
        var options = {
          chart: {
            title: 'Students performance',
            subtitle: ['Tnf' , 'mcq','desc']
          },
          bars: 'vertical' // Required for Material Bar Charts.
        };
        var chart = new google.charts.Bar(document.getElementById('barchart_material'));
        chart.draw(data, google.charts.Bar.convertOptions(options));
      }
    </script>
  </head>
  <body>
    <div id="barchart_material" style="width: 80%; height: 500px;"></div>
    <div id="piechart_3d" style="width: 900px; height: 500px;"></div>
  </body> <head>
   <% var j=0,x=0;fail=0,fs=0,se=0,ex=0 %> 
   <% for(j=0;i<lenn;i++){ %>
   <% x=  tnfmarks[j].tnfstudentmarks  %>
   <%  if(x>0){ %>
   <% fs +=1; %>
   <% } else{ %>
   <% se +=1; } %>
   <% } %>
   <script>var a= 9;var b= <%= se %>; </script>
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript">
      google.charts.load("current", {packages:["corechart"]});
      google.charts.setOnLoadCallback(drawChart);
      function drawChart() {
        var data = google.visualization.arrayToDataTable([
          ['Grade', 'Marks'],          ['Fail',    a],
          ['40-60',     4],
              ]);
        var options = {
          title: 'Marks Statitics',
          is3D: true,
        };
        var chart = new google.visualization.PieChart(document.getElementById('piechart_3d'));
        chart.draw(data, options);
      }
    </script>
</html>




